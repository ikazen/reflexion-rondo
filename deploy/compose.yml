services:
  rondo-daemon:
    image: registry.internal:80/reflexion-rondo/daemon:latest
    network_mode: host
    env_file:
      - /var/lib/rondo/.env
    environment:
      DAEMON_API_HOST: "0.0.0.0"
      PYTHONUNBUFFERED: "1"
    restart: always
